# KOL IDS V24 — Campaign Reference UI

This build makes the post-login Campaign screen follow the supplied reference: Campaign Basics, Objectives (5-column grid), Budget & Timeline, Brand Profile, and Brand Intelligence with the same field structure and hierarchy.

The app.js cache-bust query is updated so browsers do not silently reuse the previous Campaign UI build.
