# KOL IDS V25 — Campaign UI + Workspace Route

- Removes workspace logo graphic and step icons.
- Required fields use red asterisk `*`.
- Brand Values and Brand Avoid are optional free-text fields.
- Currency includes 15 major market currencies.
- Campaign typography increased for readability.
- Authenticated workspace normalizes to `/KOLIDSworkspace`.
- Netlify `_redirects` maps `/KOLIDSworkspace` to the workspace app without exposing the legacy `/KOLIDS` URL.
